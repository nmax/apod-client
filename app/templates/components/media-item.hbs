{{#if isImage}}
  {{#if isLoading}}
    {{ember-spinner}}
  {{else}}
    <img src="{{imageData}}" data-apod-id="{{modelId}}">
  {{/if}}
{{/if}}

{{#if isNoImage}}
<iframe data-apod-id="{{modelId}}" type="text/html" width="800" height="400" src="{{src}}" frameborder="0"/>
{{/if}}
